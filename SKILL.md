---
name: math-modeling-paper
description: 生产国家级奖项水平的全国大学生数学建模竞赛（CUMCM/高教社杯）完整解题论文：从赛题 PDF 解析、附件数据真实计算、GitHub 开源案例调研、顶刊文献真实性核验，到 LaTeX（国赛格式）排版、数值一致性 QA、视觉验收循环与评审标准评分。当用户要求"写数模论文"、"做 X 题的完整解答论文"、"把解题过程写成国奖格式论文"、"仿照某获奖论文写一篇"时使用本技能。
---

# 数学建模竞赛论文生产技能

本技能封装了一条经过实战验证（2025 高教社杯 B 题《碳化硅外延层厚度的确定》，23 页成品对齐国一范文）的完整论文生产线。核心原则只有一条：

> **论文里的每一个数字都必须来自你自己对附件数据的真实计算，每一条参考文献都必须核验过真实性。** 编造数据或文献是国奖论文的死刑。

## 九阶段工作流总览

详细操作见 `references/workflow.md`，历史踩坑见 `references/pitfalls.md`。

| 阶段 | 产出 | 关键动作 |
|---|---|---|
| 1. 题目解析 | 赛题全文文本 | PDF 提取（pdfplumber/pymupdf），明确三问的物理量与交付物 |
| 2. 数据勘察 | 数据结构报告 | pandas 读附件：形状、量纲、采样区间、异常波段 |
| 3. 案例与文献调研 | 方法清单 + 可引用文献表 | GitHub API 搜开源实现并读 README/解题文档；Crossref API 逐条核验文献 DOI |
| 4. 真实计算 | results.json + 全部图表 | Python 科学计算栈；先做基线方法再自研改进；每个数值可复现 |
| 5. 科研配图 | figs/*.png (200dpi) | matplotlib 中文字体配置；每图回答一个独立问题 |
| 6. LaTeX 生产 | main.tex | 国赛格式（无封面、首页摘要带数值、上标引用、三线表、代码附录） |
| 7. QA 流水线 | 零警告 PDF | check-tex → Tectonic 两遍 → font.check → pdf_qa |
| 8. 视觉验收循环 | overall pass | 全页渲染 PNG → judge 逐页审 → 修复 → 只复审改动页，直至 pass（无 judge 时用 self-check.md 人工清单） |
| 9. 评分与交付 | 评分表 + PDF/tex/代码 | 按评审 rubric 打分，指出距国一的差距清单 |
| 10. 导师/评委意见定点修订 | vN+1 版 + 修订对照表 | 意见解码→范文深读→修改矩阵→归因实验先行→定点改写→回档快照（见 references/revision-workflow.md） |

## 硬规则（违反任何一条 = 返工）

1. **数值可复算**：摘要、正文、图表中的同一量必须完全一致（小数位对齐）；写完后做一次"正文-表格数字互查"，每处百分比要能从表内数据复算出来。
2. **文献全核验**：用 Crossref API（`api.crossref.org/works/<DOI>` 或 `query.bibliographic`）核对每条文献的作者/刊名/卷期页；核验不过的条目直接剔除，绝不虚构。GitHub 实现按 GB/T 7714 [EB/OL] 引用并标注更新日期。
3. **物理量纲三查**：每个公式落地成代码时检查（a）单位换算（cm↔μm 尤其高频出错）；（b）半级/整级（峰谷交替 vs 峰峰）；（c）近似公式与精确公式的适用条件。本技能历史 bug 中 60% 是量纲问题。
4. **先基线后改进**：先复现一条文献基线方法并跑通，再引入自己的改进；改进必须以"与基线的数值对比"呈现，不能只自说自话。
5. **LaTeX 中文规范**：中文引号用 Unicode “ ”（禁 ASCII "）；下划线在正文用 `\_`（URL 里不用）；xurl 处理 URL 断行；hyperref 最后加载。
6. **图不撒谎**：每张图的数据都能追溯到 results.json；示意图（TikZ）标注必须与公式定义逐项一致（历史翻车点：厚度标注跨错层）。
7. **验收以用户可感知质量为准**：渲染 PNG 交视觉审稿，正文-图表矛盾、图内标注错位、空节浮动都是必修项；nice_to_have 可留。
8. **诚实呈现离散**：不同方法/不同折射率模型的系统差异（通常 2%–5%）要量化写进"模型不足"，这比藏起来更加分。
9. **交接摘要纪律**（跨对话/长任务）：每阶段结束输出"已完成｜关键数值锁定｜图已产出｜下一阶段｜待确认事项"五段式摘要；修改任何锁定数值必须先声明旧值→新值并同步全部已写章节。
10. **无 LaTeX 环境的交付**：产出单文件 main.tex + figs/ 打包，用户走 Overleaf（XeLaTeX 编译器）拿 PDF；验收用 references/self-check.md 的 25 条人工清单（A 档一票否决）。
11. **版本回档**：每轮成稿在 examples/<题目>/versions/ 存 `vN_页数_名称.tex+.pdf` 成对快照；修订轮先跑归因实验锁定新数值，再动笔改写。
12. **图内文字不硬编码文献编号**：matplotlib 图里的标签用实现名称（"GMM 统计实现"），不用"[15]"式编号——文献表一变图就错位。

## 交付物清单

- `final.pdf`（成品论文）
- `main.tex`（LaTeX 源码，可改可重编译）
- `analysis*.py`（全部数值的复现代码）+ `results*.json`（关键数值）
- `figs/*.png`（200 dpi 图源）
- 评分评测表（七个维度，对标国奖 rubric）
